[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: the app is a single-page React build, so every path
# must serve index.html or deep links / refreshes will 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
